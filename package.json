{
  "name": "rebeca-contract",
  "version": "1.3.1",
  "description": "Contrato del API de Rebeca: openapi.yaml + tipos TypeScript generados + mocks MSW. Se consume por versión: npm i github:CYaranga/rebeca-contract#v1.0.0",
  "type": "module",
  "files": ["openapi.yaml", "types", "mocks"],
  "exports": {
    ".": { "types": "./types/index.d.ts" },
    "./types": { "types": "./types/index.d.ts" },
    "./mocks": { "types": "./mocks/handlers.ts", "default": "./mocks/handlers.ts" },
    "./openapi.yaml": "./openapi.yaml"
  },
  "scripts": {
    "build": "npm run build:types && npm run build:mocks && npm run check",
    "build:types": "openapi-typescript openapi.yaml -o types/index.d.ts",
    "build:mocks": "node scripts/gen-mocks.mjs",
    "check": "tsc --noEmit -p tsconfig.json",
    "lint": "redocly lint openapi.yaml --skip-rule operation-4xx-response --skip-rule no-unused-components --skip-rule operation-description --skip-rule tag-description --skip-rule info-license"
  },
  "peerDependencies": { "msw": ">=2" },
  "peerDependenciesMeta": { "msw": { "optional": true } },
  "devDependencies": {
    "@redocly/cli": "^2",
    "msw": "^2",
    "openapi-typescript": "^7",
    "typescript": "^5",
    "yaml": "^2"
  },
  "license": "UNLICENSED"
}
